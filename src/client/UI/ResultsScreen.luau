-- Client-side UI component that will display the Sync Score and its breakdown
-- (mistakes, time elapsed, and penalties applied) at the end of a round.

local ResultsScreen = {}

return ResultsScreen
